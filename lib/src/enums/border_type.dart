enum PerforatedBorderType { zigzag, wavy }
// Enum for defining which sides of the border to apply the perforation
enum BorderSideType {
  left,
  right,
  top,
  bottom,
  horizontal,
  vertical,
  allSides,
}